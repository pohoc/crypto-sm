name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  full-ci:
    uses: ./.github/workflows/_ci.yml
    secrets: inherit
